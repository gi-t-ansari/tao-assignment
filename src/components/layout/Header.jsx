import React from "react";

const Header = () => {
  return (
    <header className="w-full h-fit flex justify-between items-center">
      <section>Filters section</section>
      <section>Search and Add Task section</section>
    </header>
  );
};

export default Header;
